export enum UploadStatus {
  PENDING = 'pending',
  UPLOADING = 'uploading',
  PAUSED = 'paused',
  RUNNING = 'running',
  CANCELED = 'canceled',
  UPLOADED = 'success',
  PROCESSING = 'processing',
  COMPLETE = 'complete',
  ERROR = 'error',
}
